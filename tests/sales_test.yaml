version: 2

models:
  - name: finalAnalysis
    description: "Final sales model with aggregated revenue"
    tests:
      - not_null:
          column_name: country
      - not_null:
          column_name: total_orders